# Allowlisted GO vuln IDs only (one ID per line; # comments and blanks ignored).
# Prefer fixing reachable findings (toolchain / dependency bumps). Add an ID here
# only when there is no module-path fix and the residual is documented in
# docs/security-defaults.md. Keep this file empty when none apply.
#
# After migrating nested compute from github.com/docker/docker to
# github.com/moby/moby/client + github.com/moby/moby/api, no Docker Engine
# Fixed-N/A IDs apply on the client module path. Leave empty unless a new
# residual with no module-path fix appears.
